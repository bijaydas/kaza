@props(['label' => '', 'value' => ''])

<div class="flex flex-col">
    <span class="text-zinc-400 text-xs uppercase">{{ $label }}</span>
    <span class="text-zinc-700 text-sm">{{ $value }}</span>
</div>
